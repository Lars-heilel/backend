model User {
  id          String   @id @unique @default(uuid())
  email       String   @unique
  password    String
  name        String
  isConfirmed Boolean  @default(false)
  createdAt   DateTime @default(now())
  tokens      Token[]
}
